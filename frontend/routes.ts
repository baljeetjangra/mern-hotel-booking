export const publicRoutes = ["/", "/search", "/hotels"];

export const authRoutes = ["/auth/signin", "/auth/signup"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/";
